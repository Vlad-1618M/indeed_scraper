# Dev / CI — install AFTER requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Run tests (pytest, NOT pip — --cov is a pytest flag):
#   pytest tests/ -q --cov=modules.job_store --cov=modules.job_report_api --cov-fail-under=60
# Or:  bash scripts/run_tests.sh

pytest>=8.0
pytest-cov>=4.0
coverage>=7.0
httpx>=0.27
