[pytest]
testpaths = tests
pythonpath = .
# SeleniumBase (from requirements.txt) registers a pytest plugin that conflicts
# with other plugins on --browser; our tests are plain unit/API tests.
addopts = -p no:seleniumbase
filterwarnings =
    ignore::DeprecationWarning
